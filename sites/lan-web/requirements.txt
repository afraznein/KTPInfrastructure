fastapi>=0.110
uvicorn[standard]>=0.27
authlib>=1.3
httpx>=0.27
itsdangerous>=2.1
pymysql>=1.1
jinja2>=3.1
python-multipart>=0.0.9
# optional: gallery thumbnails. Without it uploads still work, the grid
# just loads originals (app/photos.py degrades rather than failing).
pillow>=10.0
