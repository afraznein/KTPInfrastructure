# KTP Build Environment Configuration
# Copy this file to .env and customize as needed

# Path to KTP project root (containing all component directories)
# In Docker on Windows, this will be mounted from the host
KTP_PROJECT_ROOT=/mnt/ktp

# Build version tag (used for artifact directory naming)
# Default: current date in YYYYMMDD format
VERSION=

# Number of parallel build jobs
# Default: auto-detect (nproc)
BUILD_JOBS=

# Output directory for build artifacts (relative to KTPInfrastructure)
ARTIFACTS_DIR=./artifacts
