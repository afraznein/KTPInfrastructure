; KTPAMXX Module Configuration
; Profile: LAN (offline mode)
;
; Same modules as online - curl module will gracefully fail on missing URLs

fun_ktp_i386.so
engine_ktp_i386.so
fakemeta_ktp_i386.so
reapi_ktp_i386.so
dodx_ktp_i386.so
amxxcurl_ktp_i386.so       ; Kept for API compatibility, requests become no-ops
