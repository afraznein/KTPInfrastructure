; KTPFileChecker file consistency config
; Profile: Local Development
;
; Empty file means no file consistency enforcement — KTPFileChecker still
; loads, just doesn't enforce any client-file hashes. Production servers
; ship a populated version with real expected hashes.
