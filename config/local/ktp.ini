; KTPMatchHandler core config
; Profile: Local Development (smoke-test friendly)
;
; season_active=false disables the .ktp competitive password gate so
; smoke-test boots don't require a real password. Other modes (.draft,
; .12man, .scrim) are unaffected.

season_active = false
match_password = changeme
