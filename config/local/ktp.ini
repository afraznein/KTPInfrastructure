; KTPMatchHandler core config
; Profile: Local Development (smoke-test friendly)
;
; Lane B deliberately exercises the real .ktp password and season gates.
; The test-only .testmatch command reads this disposable password and submits
; it through the normal chat flow; no human or production credential is used.

season_active = true
match_password = changeme
