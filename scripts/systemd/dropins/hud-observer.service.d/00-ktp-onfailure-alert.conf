[Unit]
# ktp-systemd-alert reads its relay credentials from /etc/ktp/discord-relay.conf at
# runtime, so no secret lives in the alerting path.
OnFailure=ktp-systemd-alert@%n.service
StartLimitBurst=10
StartLimitIntervalSec=300
