# Ingest HLTV viewer-session log lines into hlstatsx.ktp_hltv_viewer_hits.
# Idempotent (INSERT IGNORE on a unique key), so a missed or doubled run is fine.
*/15 * * * *  root  /usr/local/bin/ktp-hltv-viewer-ingest.py >> /var/log/ktp-hltv-viewer-ingest.log 2>&1
